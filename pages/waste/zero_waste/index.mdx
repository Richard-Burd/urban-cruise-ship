import ZeroWaste from "./indexcomponent.js";
import ArticleImage from "/components/ArticleImage.js";

<ZeroWaste />

{/* Michael & Lee can change this to the correct site */}
export const site = "waste";

<div className={`${site}-background-gradient`}>
{/* In order to make this article, a system for making the Solutions dropdown boxes has to be devised */}
<br></br>
</div>
