import Article from '/components/Article.js'
import hierarchy from "../hierarchy.json";

export const site = "space";
export const focusAreaUrl = "ethics";

import ArticleImage from "/components/ArticleImage.js";
import PostHarvestLossTanzania from "/solutions/post-harvest-loss-tanzania.mdx"


# A Test Page

<PostHarvestLossTanzania />
export default ({ children }) => 
  <Article 
    focusAreaUrl={focusAreaUrl} 
    hierarchy={hierarchy} 
    site={site}
  >
    {children}
  </Article>