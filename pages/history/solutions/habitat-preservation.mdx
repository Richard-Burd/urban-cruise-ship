import Article from '/components/Article.js';
import hierarchy from "../hierarchy.json";
import SolutionTable from "../../../components/SolutionTable";
import { solutionHabitatPreservation } from "../../../lib/dynamicBarCharts/solutionProc";

import dynamic from "next/dynamic";

export const site = "history";
export const focusAreaUrl = "solutions";

export const DynamicSingleBarChart = dynamic(
  () => import("../../../components/DynamicSingleBarChart"),
  { ssr: false }
);

<DynamicSingleBarChart
  barChartTitle={"Solution Habitat Preservation"}
  barChartSubTitle={"values shown are in square kilometers"}
  scale={"positive"}
  rightSide={80}
  leftSide={455}
  totalWidth={1000}
  solutionBackgroundWidth={610}
  solutionBackgroundOffset={-605}
  labelText={10}
  labelAnchor={"start"}
  titleText={0}
  titleAnchor={"end"}
  fetchDataFunc={solutionHabitatPreservation}
/>

<SolutionTable />

export default ({ children }) => 
  <Article 
    focusAreaUrl={focusAreaUrl} 
    hierarchy={hierarchy} 
    site={site}
  >
    {children}
  </Article>