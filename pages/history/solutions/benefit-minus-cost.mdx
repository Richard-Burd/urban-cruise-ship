import Article from '/components/Article.js';
import hierarchy from "../hierarchy.json";
import SolutionTable from "../../../components/SolutionTable";
import { solutionBenefitMinusCost } from "../../../lib/dynamicBarCharts/solutionProc";

import dynamic from "next/dynamic";

export const site = "history";
export const focusAreaUrl = "solutions";

export const DynamicSingleBarChart = dynamic(
  () => import("../../../components/DynamicSingleBarChart"),
  { ssr: false }
);

### Here, we compare our proposed solutions on the basis of net benefit.

<DynamicSingleBarChart
  barChartTitle={"Solutions - Net Benefit (Benefit Minus Cost)"}
  barChartSubTitle={""}
  scale={"positive"}
  rightSide={75}
  leftSide={425}
  titleText={0}
  fetchDataFunc={solutionBenefitMinusCost}
/>
<DynamicSingleBarChart
  barChartTitle={""}
  scale={"negative"}
  rightSide={295}
  leftSide={-50}
  solutionBackgroundOffset={0}
  titleText={495}
  fetchDataFunc={solutionBenefitMinusCost}
/>
<br/>
## Interactive Table
### Sort this table by clicking on the black column headings
<br/>
<SolutionTable />

export default ({ children }) => 
  <Article 
    focusAreaUrl={focusAreaUrl} 
    hierarchy={hierarchy} 
    site={site}
  >
    {children}
  </Article>