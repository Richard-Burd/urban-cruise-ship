import Article from "/components/Article.js";
import hierarchy from "../hierarchy.json";
import SolutionEndeavorTable from "../../../components/SolutionEndeavorTable";
import { solutionEndeavorBenefitOverCost } from "../../../lib/solutionEndeavorProc";

import dynamic from "next/dynamic";

export const site = "history";
export const focusAreaUrl = "solutions-and-endeavors";

export const DynamicSingleBarChart = dynamic(
  () => import("../../../components/DynamicSingleBarChart"),
  { ssr: false }
);

<DynamicSingleBarChart
  barChartTitle={"Solution & Endeavor Efficiency / Benefit minus Cost"}
  barChartSubTitle={"cost shown are rations of benefit over cost"}
  scale={"positive"}
  barHeight={4002}
  rightSide={55}
  leftSide={455}
  totalHeight={4000}
  totalWidth={1000}
  solutionBackgroundWidth={610}
  solutionBackgroundOffset={-605}
  labelText={10}
  labelAnchor={"start"}
  titleText={0}
  titleAnchor={"end"}
  fetchDataFunc={solutionEndeavorBenefitOverCost}
/>

<SolutionEndeavorTable />

export default ({ children }) => 
  <Article 
    focusAreaUrl={focusAreaUrl} 
    hierarchy={hierarchy} 
    site={site}
  >
    {children}
  </Article

>
