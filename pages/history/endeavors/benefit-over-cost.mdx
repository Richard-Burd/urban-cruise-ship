import Article from '/components/Article.js';
import hierarchy from "../hierarchy.json";
import EndeavorTable from "../../../components/EndeavorTable";
import { endeavorBenefitOverCost } from "../../../lib/dynamicBarCharts/endeavorProc";

import dynamic from "next/dynamic";

export const site = "history";
export const focusAreaUrl = "endeavors";

import FullSetOfEndeavorReferences from "/endeavors/endeavor-references.mdx";

export const DynamicSingleBarChart = dynamic(
  () => import("../../../components/DynamicSingleBarChart"),
  { ssr: false }
);

<DynamicSingleBarChart
  barChartTitle={"Endeavor Efficiency - Benefit Over Cost Ratio"}
  barChartSubTitle={"values shown below are ratios of benefit over cost"}
  scale={"positive"}
  barHeight={3500}
  rightSide={55}
  leftSide={455}
  totalHeight={3502}
  totalWidth={1000}
  solutionBackgroundWidth={610}
  solutionBackgroundOffset={-605}
  labelText={10}
  labelAnchor={"start"}
  titleText={0}
  titleAnchor={"end"}
  fetchDataFunc={endeavorBenefitOverCost}
/>
<DynamicSingleBarChart
  barChartTitle={""}
  scale={"negative"}
  barHeight={80}
  rightSide={485}
  leftSide={-20}
  totalHeight={82}
  totalWidth={1000}
  solutionBackgroundWidth={610}
  solutionBackgroundOffset={605}
  labelText={-10}
  labelAnchor={"end"}
  titleText={495}
  titleAnchor={"start"}
  fetchDataFunc={endeavorBenefitOverCost}
/>

<EndeavorTable />

<FullSetOfEndeavorReferences />

export default ({ children }) => 
  <Article 
    focusAreaUrl={focusAreaUrl} 
    hierarchy={hierarchy} 
    site={site}
  >
    {children}
  </Article>