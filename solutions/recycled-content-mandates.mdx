import SolutionDropdown from '/components/SolutionDropdown.js'

export const problem = "Material Loss";
export const solution = "Recycled Content Mandates";

We propose that states establish recycled content mandates to the extent that they are financially feasible.

export default ({ children }) => 
  <SolutionDropdown 
    problem={problem} 
    solution={solution}
  >
    {children}
  </SolutionDropdown>