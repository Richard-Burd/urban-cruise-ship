import SolutionDropdown from '/components/SolutionDropdown.js'

export const problem = "Damage from Orbital Debris";
export const solution = "Orbital Use Fee";

We propose tighter regulations for end-of-life management of commercial satellites, particularly enforcement of deorbiting rules for low-Earth orbit satellites.

export default ({ children }) => 
  <SolutionDropdown 
    problem={problem} 
    solution={solution}
  >
    {children}
  </SolutionDropdown>