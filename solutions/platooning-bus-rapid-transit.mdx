import SolutionDropdown from '/components/SolutionDropdown.js'

export const problem = "Cost of Rail";
export const solution = "Platooning Bus Rapid Transit";

Vehicle platooning is a technology that has been developed for freight trucking, and may be easier to deploy than fully autonomous vehicles. Platooning buses in dedicated lanes can match the low cost and flexibility of BRT with the throughput of metro rail.

export default ({ children }) => 
  <SolutionDropdown 
    problem={problem} 
    solution={solution}
  >
    {children}
  </SolutionDropdown>