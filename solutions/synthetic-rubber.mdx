import SolutionDropdown from '/components/SolutionDropdown.js'

export const problem = "Impacts of Natural Rubber";
export const solution = "Synthetic Rubber";

Industry should expand the use of synthetic rubber.

export default ({ children }) => 
  <SolutionDropdown 
    problem={problem} 
    solution={solution}
  >
    {children}
  </SolutionDropdown>