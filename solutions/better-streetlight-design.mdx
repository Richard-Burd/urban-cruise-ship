import SolutionDropdown from '/components/SolutionDropdown.js'

export const problem = "Light Pollution";
export const solution = "Better Streetlight Design";

We propose that cities design streetlights to minimize skyglow.

export default ({ children }) => 
  <SolutionDropdown 
    problem={problem} 
    solution={solution}
  >
    {children}
  </SolutionDropdown>