import SolutionDropdown from '/components/SolutionDropdown.js'

export const problem = "Need for Load Balancing";
export const solution = "Research and Development into Storage";

## Description

The Department of Energy should fund research and development for promising energy storage options.

## Background

Potential options including compressed air, novel electrochemistries, flywheels, and capacitors. Other options, such as vehicles as storage and betavoltaics, appear to be less promising.

export default ({ children }) => 
  <SolutionDropdown 
    problem={problem} 
    solution={solution}
  >
    {children}
  </SolutionDropdown>