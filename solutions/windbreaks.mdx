import SolutionDropdown from '/components/SolutionDropdown.js'

export const problem = "Erosion";
export const solution = "Windbreaks";

We propose that methods to reduce soil erosion, such as windbreaks, become more widespread. Such practices could be promoted by the U. S. Department of Agriculture via education.

export default ({ children }) => 
  <SolutionDropdown 
    problem={problem} 
    solution={solution}
  >
    {children}
  </SolutionDropdown>