import SolutionDropdown from '/components/SolutionDropdown.js'

export const problem = "Land Use from Agriculture";
export const solution = "Agroforestry";

The U. S. Department of Agriculture should conduct research into cheaper agriforestry methods and disseminate the techniques to do so through education outreach

export default ({ children }) => 
  <SolutionDropdown 
    problem={problem} 
    solution={solution}
  >
    {children}
  </SolutionDropdown>