import SolutionDropdown from '/components/SolutionDropdown.js'

export const problem = "Space and Energy of Driving";
export const solution = "Maglev Elevators";

Maglev elevators, or cord-free elevators, are an emerging technology that can better utililize scarce downtown space.

export default ({ children }) => 
  <SolutionDropdown 
    problem={problem} 
    solution={solution}
  >
    {children}
  </SolutionDropdown>