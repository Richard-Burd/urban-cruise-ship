import SolutionDropdown from '/components/SolutionDropdown.js'

export const problem = "Need to Promote Active Transportation";
export const solution = "Pedestrian Skybridges";

Skybridges can be an effective tool to reduce congestion, improve safety, and promote walking in high density areas.

export default ({ children }) => 
  <SolutionDropdown 
    problem={problem} 
    solution={solution}
  >
    {children}
  </SolutionDropdown>